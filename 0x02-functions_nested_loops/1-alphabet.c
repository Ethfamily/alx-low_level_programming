#include <stdio.h>
#include "main.h"
/**
*main-Entry point
*
*function print_alphabet:builtin function
*
*Return:lways 0 (Success)
*/
char print_alphabet(void);
int main(void)
{
	print_alphabet(b);
	return (0);
}
char print_alphabet(void)
{
	int = i;
	for (i = 97; i < 124; i++)
	{
		char c = (char)i

		putchar(c);
	}
	return (0);
}

#include "main.h"
/**
 * add - function that adds teo integers together
 * @i: takes in an integer
 * @j: takes in an integer
 * Return: i + j result
 */
int add(int i, int j)
{
	int c = i + j;

	return (c);
}

#include "main.h"
/**
 * leet - encode
 * @s: pointer to char
 * Return: *s
 */
char *leet(char *s)
{
	int i, j, k;
	if ((i == 76 || i ==102) && (j == 69 || j ==101) && (k == 69 || k == 101) && (l == 89 || l == 116))
		printf("%c%c%c%c", i , j, k, l);
}

#include "main.h"
/**
 * _strcmp - compare
 * @s1 : pointer to car
 * @s2 : pointer to char
 * Return: Alway 0
 */
int _strcmp(char *s1, char *s2)
{
	int i;
	int R;

	i = 0;

	if(i < j)
		printf("i is less than j");
	else if(i > j)
		printf("i is greater than j");
	else
		printf(" both i and j are the same");
}
